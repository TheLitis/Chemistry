# R07 target-domain execution

Base: 02ad7ff5ec7302f0458377c77f01ba5bf7bb788e. Continue the prepared and user-approved R07 package.

1. Verify the existing source package and its target-domain regression/integration tests.
2. Publish only the new target-domain module, task, and tests; preserve runner configuration and existing models.
3. Run ChemistryPC training from scratch with all 250 NP target structure keys excluded across all training libraries. Retain the complete independently retrained V1 recipe as control.
4. Use 80 calibration keys (including previously examined target keys) and 170 audit keys. Freeze the choice before final ranks. Evaluate reference-present, reference-absent, and real external-catalog recovery separately. Catalog excision is not de novo generation.
5. Independently inspect returned training reports, raw ranks, input/model hashes, and failure diagnostics. Do not equate passing software tests with chemical accuracy.
6. Build a deployment candidate only when the evidence supports it. Keep V1 as official champion until a genuine evaluated submission improves its score.

No Kaggle upload or submission is authorized by the experiment task itself. Any later submission must separately inspect current rules, notebook output, account history, and quota. No hidden answers, test-label features, credential logging, system permission changes, or model overwrites.

Current experiment status at publication: prepared, not trained on the full CASMI corpus.

# R08B forward-assisted R07 release

**Goal:** Independently verify the completed prospective R08B experiment, then build and execute a separate release using the unchanged full-refit R07 assets plus fixed FIORA evidence.

**Approved scope:** Continue the user's requested full R07 versus R07 plus forward-spectrum comparison. No coefficient search on Kaggle; preserve R07 (official public score 0.188) unchanged.

**Implementation:** Reuse `r07_release` for candidate construction and base scores. Expose an optional rank callback with default behavior numerically unchanged. A separate forward candidate module evaluates the frozen `base + 0.25*cosine_nearest` score using certified top-25 bounds; unsupported forward conditions contribute zero. Store model/source/input identities and evaluation counts. The candidate must not depend on cached visible test answers. Full final inference uses the frozen full-data R07 checkpoint, not the validation checkpoint.

## Tasks
- [ ] Verify downloaded R08B artifact SHA256, all report file hashes, all 672 numerical certificates, raw ranks, intervals and prospective gate. Preserve the earlier failed R08 gate.
- [ ] Test optional ranking callback: default CSV parity, exact candidate alignment, same-group raw inputs, invalid callback rejection, no hidden-label reads, complete/nonempty output.
- [ ] Implement bounded forward callback and a separate entry point. Pin FIORA source and weights. Do not modify model weights or coefficients.
- [ ] Execute full tests on ChemistryPC; inspect latest account status and official file metadata read-only. Organizers posted a training-data correction, so record the actual local and remote provenance before a new notebook.
- [ ] Run full visible-data inference from a clean versioned release. Independently verify structural validity, all IDs, all spectra and hash agreement. Keep default R07 output intact.
- [ ] Package and validate offline Linux dependencies and private Kaggle preview before any scored submission. Shared two-per-rolling-24-hour gate and at-most-once journal apply.

## Evidence constraints
R08B's fixed score was chosen before its new 128 timsTOF and 96 externally covered queries. Its external cohort is conditional on coverage in a previous COCONUT slice and uses mixed instruments. FIORA pretraining membership is unknown. This is not de novo identification. A passed local gate, completed tests, and valid CSV are not an official Kaggle improvement. Report any unfinished deployment steps precisely; never relabel them completed.
